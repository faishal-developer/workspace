// for maintainance we should update environment variable. it will show maintainance page dynamically 
export const config = {
    Maintain: process.env.REACT_APP_MAINTANANCE,
    
};

console.log(config);